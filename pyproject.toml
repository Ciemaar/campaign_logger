[build-system]
requires = [
    "setuptools>=30.3.0",
    "wheel",
]

[tool.black]
line-length = 140
target-version = ['py311']
skip-string-normalization = true

[project]
name = "campaign-logger"
version = "0.0.0"
