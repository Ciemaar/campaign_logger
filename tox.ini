[tox]
envlist =
    clean,
    check,
    docs,
    py311-cover,
    py311-nocov,
    py312-cover,
    py312-nocov,
    py313-cover,
    py313-nocov,
    py314-cover,
    py314-nocov,
    report

[testenv]
basepython =
    {bootstrap,clean,check,report,docs,codecov,coveralls}: {env:TOXPYTHON:python3}
setenv =
    PYTHONPATH={toxinidir}/tests
    PYTHONUNBUFFERED=yes
passenv =
    *
deps =
    pytest
extras =
    test
commands =
    {posargs:pytest -vv --ignore=src}

[testenv:bootstrap]
deps =
    jinja2
    matrix
skip_install = true
commands =
    python ci/bootstrap.py --no-env

[testenv:check]
deps =
    docutils
    check-manifest
    readme-renderer
    pygments
    ruff
    pyright
    mdformat
    setuptools<70.0.0
    build
    twine
    jinja2
    matrix
    sphinx
    sphinx_py3doc_enhanced_theme
    pytest
    pytest-cov
    requests-mock
    click
    pydantic
    requests
skip_install = true
commands =
    python -m build --sdist --wheel .
    twine check dist/*
    check-manifest {toxinidir}
    ruff check .
    ruff format --check .
    pyright .
    mdformat --check --wrap 80 README.md SESSION_INSTRUCTIONS.md PYPI_UPLOAD_INSTRUCTIONS.md AGENTS.md .github/copilot-instructions.md docs/tooling_evaluation.md

[testenv:docs]
usedevelop = true
deps =
    -r{toxinidir}/docs/requirements.txt
commands =
    sphinx-build {posargs:-E} -b doctest docs dist/docs
    sphinx-build {posargs:-E} -b html docs dist/docs
    - sphinx-build -b linkcheck docs dist/docs


[testenv:report]
deps = coverage
skip_install = true
commands =
    coverage report
    coverage html

[testenv:clean]
commands = coverage erase
skip_install = true
deps = coverage

[testenv:py311-cover]
setenv =
    {[testenv]setenv}
usedevelop = true
commands =
    {posargs:pytest -vv --cov=campaign_logger --cov-fail-under=95 --cov-report=term-missing}
deps =
    {[testenv]deps}
    pytest-cov

[testenv:py311-nocov]

[testenv:py312-cover]
setenv =
    {[testenv]setenv}
usedevelop = true
commands =
    {posargs:pytest -vv --cov=campaign_logger --cov-fail-under=95 --cov-report=term-missing}
deps =
    {[testenv]deps}
    pytest-cov

[testenv:py312-nocov]

[testenv:py313-cover]
setenv =
    {[testenv]setenv}
usedevelop = true
commands =
    {posargs:pytest -vv --cov=campaign_logger --cov-fail-under=95 --cov-report=term-missing}
deps =
    {[testenv]deps}
    pytest-cov

[testenv:py313-nocov]

[testenv:py314-cover]
setenv =
    {[testenv]setenv}
usedevelop = true
commands =
    {posargs:pytest -vv --cov=campaign_logger --cov-fail-under=95 --cov-report=term-missing}
deps =
    {[testenv]deps}
    pytest-cov

[testenv:py314-nocov]
